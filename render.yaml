services:
  - type: web
    name: save-media
    runtime: image
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO_NAME
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_VERSION
        value: 18.17.0